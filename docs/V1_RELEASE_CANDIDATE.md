# 观辰 v1 网页端发布候选验收记录

状态：网页端本轮计划开发与自动回归已收尾，等待完整使用验收。尚未正式冻结，没有创建 v1.0.0 标签。

最新补充：后续已完成独立真实网页/后端验收及问题整改，见 [验收清单](./QA_FULL_TEST_CHECKLIST.md) 与 [整改报告](./QA_REMEDIATION_REPORT.md)。当前回归为 78 项通过，依赖告警已清零。下列阶段记录保留原时间点的验证方式，不代表最新覆盖范围。

## 范围与阶段提交

沿用 V1_FREEZE_SCOPE.md 的范围：保留 M0–M9、既有前端改进及备份能力，不增加 M10，不扩展备份功能。

| 阶段 | 提交 | 结果 |
| --- | --- | --- |
| 八字时间入口 | 646a593 | 默认流年按立春区间定位，流日使用引擎有效区间，手动选择绑定当前排期 |
| 八字页面与追问 | 594d8a1 | 所选日期进入聊天草稿；追问沿用用户范围；无效日期及越界年份报错；一月日分析使用所属流年 |
| 生时校正 | d5811a3 | 事件 A→B→A 后读取端匹配历史有效评估，不再陷入重复评估提示 |
| 整体验收 | 见本文件所在提交 | 全量回归入口、检查结果和发布边界记录 |

本轮开发分支为 codex/report-facts-events，每阶段均已提交并推送；未自动合并 main。

## 自动验证

早期执行 `npm run test:v1-all` 为 77 项通过；后续加入请求边界回归后，最新 78 项通过。结构化结果见 [检查结果](./v1-check-results.json)。每项使用独立进程；兜底数据库、报告输出及日志均放在独立临时目录，AI 使用模拟或环回地址，不调用真实供应商。

最终代码生产构建通过（包括类型校验），生成 72 个静态页面。

- 单盘录入、历史、上下文与连续追问；年/月/日运限及事件匹配。
- 专题报告事实、引用、版本对比、人工修订与 PDF 导出。
- 合盘角色隔离、当前与指定年度阶段、上下文和报告。
- 八字排盘、时间边界、版本绑定、各层规则证据和追问范围。
- 校时事件、候选评估、限制确认、历史恢复与工作命盘创建。
- 学习、案例、提醒、月度复盘等已实现模块。
- 聊天首字前停止、部分内容保留、断流、并发幂等、重试隔离和重启恢复。
- 独立 SQLite 实际备份、预检、整库恢复及恢复前备份；加密导出解密；单命盘关联闭包导出、导入副本及事务回滚。

这不是对真实 AI 回答质量的保证，也不表示每个模块已经完成真实设备交互验收。

## 浏览器检查及限制

独立 QA 服务启动曾被自动审批拒绝，原始返回仅为 blocked by policy。随后在已恢复的项目服务上使用浏览器请求拦截，所有 API 返回合成夹具，避免读取或写入真实档案。

已验证：

- 八字合成档案打开、选择流日、进入聊天后草稿日期一致，草稿不自动发送。
- 同一页面已有未发送草稿时，新日期草稿不覆盖当前输入，地址中的草稿参数被清理。
- 单盘历史页面重新打开、刷新后加载合成消息；Markdown 标题、粗体、列表正确渲染，桌面无横向溢出。
- 390px 模拟宽度检查八字聊天页面，没有横向溢出；不替代真实手机验收。

本次采用“浏览器合成请求 + 后端独立数据库回归”的分层验证。完整真实浏览器链路（录入→起盘→聊天→历史→事件确认→报告与导出）仍需使用验收，不将分层验证记为该门槛全部通过。以前各批次的浏览器/PDF 验证记录保留在 AI 交接文档中。

## 安装、升级与回退

安装与启动按 [README](../README.md) 执行；升级前下载并独立保存备份，停止服务并保留配置及整个数据目录，再安装依赖、构建与启动。SQLite 结构仍为 v47，本轮没有增加迁移。

回退时使用兼容提交与升级前备份，在独立目录验证，不用旧代码直接操作已迁移的数据。备份和 PDF 副本由部署者自行保管；真实数据的发布前备份留到实际冻结操作时完成，本轮合成恢复测试不冒充真实发布备份。

## 冻结前剩余验收

1. 用户完成网页端完整使用链路及真实 AI 表达质量确认。
2. 按用户安排，在网页端完成后使用真实手机和另一台局域网主机测试；响应式模拟不替代实机。
3. 对发现的阻断问题修复并回归；保存发布前备份，确定发布提交，再记录版本号并创建 v1.0.0 标签。

尚未满足以上条件，不提前声明正式冻结。新供应商深度适配、跨设备同步、新命理模块、非阻断知识扩展和全站视觉治理继续留到 v1 之后。
