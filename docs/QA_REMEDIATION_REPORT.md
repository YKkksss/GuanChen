# 全项目验收问题整改记录

本次以 [验收清单与问题报告](./QA_FULL_TEST_CHECKLIST.md) 为基线，修复可在当前网页与本机环境完成的遗留问题。所有写入和导出复测均使用独立临时 SQLite 与合成资料，不操作正式档案。没有合并 main 或创建冻结标签。

## 整改结果

| 项目 | 修改 | 验证 |
| --- | --- | --- |
| F07：38 项依赖告警 | 移除业务未使用的 Vercel CLI、遗留 Cloudflare Pages 适配器及对应命令；Next.js 嵌套 PostCSS 使用与项目一致的 8.5.28 | 完整与生产依赖 audit 均为 0；依赖树无旧 PostCSS；生产构建通过 |
| PDF 免责声明孤页 | 按实际文本高度测量尾部，短列表与免责声明一起安排，收紧章节间距；渲染版本升级至 v5，旧导出缓存不阻止重新生成 | 原校时样本 3→2 页；四类实际接口下载正常；正文仍为 12pt，白底深色、可提取文字；新增两页分页回归 |
| 八字内部英文标签 | 新计算使用年/月/日/时；引擎展示版本更新；旧快照通过展示格式化兼容 | 原局 time 等四个键的断言通过；真实历史页面展开后显示“原局时支午”；不改写历史审计指纹 |
| 事件表单交互 | 命名区域、标准 form、fieldset/legend、选中状态、错误提示；打开聚焦、Esc 关闭和焦点恢复；必要字段与反向日期校验 | 实际网页拒绝反向日期；修正后 Enter 保存返回 201，焦点回到新增事件；表单自动无障碍检查通过 |
| 记忆管理 | 命名非模态区域、可聚焦、Esc 关闭和焦点恢复；纠正文本框增加名称 | 实际打开与关闭、焦点恢复及自动无障碍检查通过 |
| 页面可读性 | 加深首页、知识库、命盘辅助文字、图例、八字五行与状态文字；取消选中标签辅助文字的透明度 | 桌面与窄屏自动对比度复测，保留各语义色区别 |
| 控件语义 | 最近命盘表格的加载/错误/空状态使用 row/cell；校时加载提示增加 status；练习选择器、窄屏首页图标补名称；八字事实区支持键盘滚动 | 真实生产页面检查 |
| 动态效果 | 命盘宫格与中心区遵循系统减少动态效果设置 | 减少动态效果环境下内容直接呈现，复测通过 |
| 首页水合警告 | 首页正文不再依赖浏览器动态偏好设置初始透明度，服务端与客户端均直接显示内容 | 开发服务开启减少动态效果后刷新，控制台错误为 0 |
| 开发访问 | 明确允许 localhost、127.0.0.1 加载开发资源 | 保留原可信局域网地址规则，未扩大为任意来源 |

前轮把人生事件侧栏表单称为“缺少 dialog 的弹窗”不准确。本次按实际页面内表单补语义，没有把它错误声明为模态。遗留 `ShareModal` 同时补充 dialog 名称、共享焦点管理和滚动锁；当前没有业务入口引用它，只计类型和构建检查，不计真实用户路径验证。

## 自动回归与实际运行

- `npm run test:v1-all`：78/78 通过；随后新增 PDF 分页断言，再单独运行 `test:report-pdf` 通过。
- `npx tsc --noEmit` 通过；最终 `npm run build` 通过，生成 72 个静态页面。
- 独立生产服务真实 HTTP：191/191；数据安全专项：19/19，包括关联归属、跨站写入拒绝、损坏备份、路径与数据库完整性。
- axe-core 4.13.0：33 个路由加八字历史依据展开状态，1440×1000 与 390×844 共 68 个检查状态。最后一次相关复测均无自动违规、页面脚本异常或文档级横向溢出；首页悬停色的最后修改单独复测，机器证据保留此前失败及后续通过记录。
- PDF：总览 3 页、合盘 3 页、校时 2 页、年度 1 页，共 9 页，均通过生产 API 生成/下载，渲染版本为 `report-pdf-v5`。逐页渲染复查，无乱码、裁切或重叠；边界、字号、颜色与文字提取另作机器检查。不同长度报告仍可能保留较短末页，不通过缩小字号强行压页。
- 聊天设置：阅读/输入/行为三个页签自动检查通过，Esc 后返回设置按钮；使用原生 modal dialog。Tab 从末个按钮短暂离开文档焦点后再次进入对话框，没有落到背景页面控件。

机器证据见 [桌面与窄屏检查](./qa-evidence/2026-09-08-remediation-accessibility.json)、[依赖与 PDF 结果](./qa-evidence/2026-09-08-remediation-checks.json)。浏览器脚本为 `scripts/qa-accessibility-browser.js`，通过 Playwright 的 `browser_run_code` 文件参数执行，目标固定为独立生产服务 127.0.0.1:30002；每次使用当前视口，桌面和窄屏分别运行。

全量回归原日志位于系统临时目录 `guanchen-v1-checks-ABDq50`；HTTP、安全与 PDF 原始结果也保留在系统临时目录。正式服务使用 30001，不包含 QA 合成资料。

## 仍需人工或外部环境验收

- 自动 axe 检查通过不等于完整 WCAG 认证；`incomplete` 项仍需人工判定，屏幕阅读器和不同浏览器内核未全面验证。
- 按用户安排，真实 AI 表达、供应商网络/限流、真实手机及另一台局域网主机留待后续测试，不能用模拟供应商或窄视口替代。
- 仍是个人及可信局域网共享数据库，不具有账号/租户隔离。本次没有新增公网多用户部署能力。
- 长时间压力、容量、断电和磁盘故障、完整渗透测试不在已通过结论内。

当前为整改后的网页候选版本。完成用户使用验收和发布前备份后，才能确定正式冻结提交。
