# 2026-09-08 工作交接

## PDF 导出清晰度优化

用户反馈专题报告 PDF 米色背景、灰色小字和字形偏细导致难以阅读。检查用户提供的四页 PDF，确认没有位图图片，属于矢量文字排版问题。

- 公用导出器升级 report-pdf-v2，旧报告再次导出会因渲染版本不同重新生成，不需要重新调用 AI，也不改报告内容。
- 去除米色卡片，使用白底与 #111111 / #171717 / #333333 深色文字；正文 12 磅，依据详情 10 磅，元信息至少 10 磅，页脚 9 磅。
- Windows 默认字体改用静态等线常规/粗体，避免可变 Noto 字体默认细字重；自定义字体配置和项目内字体优先级保留。
- 元信息取消固定高度与省略号，按实际文本换行；摘要改为正常文档流，避免长摘要跨页后的错误高度；章节依据数量另起一行，避免与长标题重叠。
- 作用于使用公用导出器的专题、合盘、年度和校时报告。已下载的旧文件不改变，需要重新导出。

验证：独立 SQLite、合成报告通过 PDF 导出、版本对比、人工修订回归。渲染五页长报告并查看首页及全页缩略图，字体大小范围 9–24 磅、文字仅三种深色，未见重叠或裁切。未外呼真实 AI，未读取真实数据库。用户 PDF 仅本地查看，未上传。

类型检查和生产构建通过，生成 72 个静态页面。


## 品牌更名为观辰

用户确定首选名称“观辰”，功能说明“命盘与人生观察”，首页标题“观星辰，知自己”。同步更新首页、工作台导航、无障碍名称、页面元信息、分享卡、报告阅读及 PDF 品牌，增加“观”字矢量标识和站点图标。手机标题按短句换行。

PDF 渲染版本升级 report-pdf-v3，旧报告重新导出即可使用新品牌，不改历史报告内容。数据目录、数据库标识、浏览器偏好键和接口保持兼容；“紫微命盘”等业务名词不机械替换。原版 README 保留，README2 品牌和四张示例截图更新，首页工作台预览同步更新。

独立示例数据库验收桌面和 390/320 宽度；PDF、导航、版本对比、人工修订回归通过。无真实 AI 调用。用户本轮明确授权开发分支推送并合并 main；此前禁止自动合并的约束在本次授权范围内解除，未声明 v1 冻结。

观辰更名生产构建通过，72 个静态页面，类型检查通过。


### 观辰标识优化

按用户反馈将单字印章替换为朱砂圆形星轨与四芒星图形，使用独立 guanchen-mark.svg 路径刷新浏览器图标。同步首页、工作台、命盘和分享卡；README2 四张截图与首页预览更新。桌面实际导航栏与 390 宽度检查通过，无横向溢出。


## 专题报告第二批：当前阶段与专题事件

### 实现

- 新增 getCurrentStage / refreshChartStage，按北京时间观察日计算农历虚岁（农历年差加一、春节换岁），与本地 iztro 默认 normal 年龄规则一致。返回分析日期、年龄口径、当前大限和农历年边界换算后的公历起止日。
- 原排盘和报告使用公历年份相减，历史页面还直接读取快照创建时的当前阶段。本批新排盘、历史命盘重新打开、完整/降级 AI 基础上下文、旧 interpret 摘要入口和新专题报告共用阶段规则。页面明确显示“当前大限 · 虚岁”；只刷新派生数据，不改写原快照或本命星曜。不在大限范围时返回空阶段，不补造童限。
- 专题报告事件分类筛选：事业优先工作/学习等，感情优先关系/家庭/子女，财富优先财务/工作/迁移，健康优先健康；自定义类别按有限关键词补充。总览和性格保留各类事件。按相关度、影响程度、日期、更新时间和 ID 稳定排序，最多 20 条。
- 当前大限报告按完整阶段日期范围与正式事件相交筛选，未知日期不归入阶段；其他专题保留相关但日期未知的记录。所有证据保留原始日期精度，不用挂接数量加权。
- 核心证据新增 eventSelection：策略、分类、已确认/匹配/选入/遗漏/截取数量及阶段范围；提示未检索到不等于未发生，粗日期相交仅表示可能相关。专题提示版本升为 topic-report-v3，要求模型说明记录缺失或截取限制。

### 验证与边界

新增 test:report-facts，合成资料覆盖农历年前出生、春节前后、公历元旦不换岁、北京时间春节零点、大限首尾、未起首限、旧快照不变、页面派生数据/两种 AI 摘要/报告一致、25 条无关事件干扰、自定义事件、确认状态、日期未知、跨阶段事件与选择上限。直接与本地 iztro 运限结果对照。

报告、上下文、出生归一化、连续追问、自然语言年度、年度/月日事件、年/月/日运限、合盘报告与校时评估回归和类型检查通过。独立示例数据库浏览器验证旧命盘正常打开、虚岁标签显示和原示例消息保留。

历史报告内容保持原样，新生成/重新生成才使用新事实。已经打开的命盘跨日后需要刷新以更新阶段。年度运限仍按既有年度代表日计算，不把报告观察日与年度代表日视为同一时点。合盘的阶段提取与所选运限逻辑留到其专项阶段核对。本批未改数据库结构（仍 v47），未读取真实档案、未调用真实 AI，不能据此宣称模型一定生成高质量报告。

下一批继续报告结论与依据的实际阅读验收，再按顺序推进合盘、八字与生时校正；备份扩展继续暂停，v1 未冻结。

专题报告第二批生产构建通过，生成 72 个静态页面。
