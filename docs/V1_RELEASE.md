# 观辰 v1.0.0 发布记录

发布日期：2026-09-09（UTC+08:00）。发布分支：`main`；冻结标签：`v1.0.0`。

## 冻结范围与确认

沿用 V1_FREEZE_SCOPE.md：面向个人与可信局域网，包含 M0–M9 和已完成的录入、聊天、事件、报告、合盘、八字、校时及数据备份能力。后续新增功能进入下一版本，v1 只处理必要修复。

用户在网页整改完成后反馈其他设备测试无问题，并明确授权冻结、推送、合并 main 和打标签。设备型号、浏览器和逐项实机记录未提供；此处记录用户验收结论，不作为自动化实机证据。真实 AI 表达质量仍取决于供应商和输入，本次不声称已另行进行真实模型专项测评。

## 验收依据

- 功能回归 78 项、HTTP 检查 191 项、数据安全检查 19 项通过。
- 68 个网页状态的自动无障碍及溢出复核通过；生产构建通过（72 个静态页面）。
- 完整与生产依赖审计在整改时均为 0 告警。
- 四类 PDF 共 9 页经过文本、颜色、边界及视觉检查；新版渲染器为 v5。
- 详细证据见 [整改报告](QA_REMEDIATION_REPORT.md)、[完整验收清单](QA_FULL_TEST_CHECKLIST.md) 和 qa-evidence 目录。上述结果对应已验证功能提交 `7370000`；发布提交仅调整版本及文档。

## 备份、升级与回退

发布前已通过应用备份服务生成真实数据库与报告附件归档，并通过归档预检；部署配置另行保存在同一份本机发布备份目录。备份位于部署用户的 `%LOCALAPPDATA%/GuanChen/release-backups/` 下，包含私密数据，不提交 Git 或上传远程。既有独立测试数据库恢复测试已通过，本次未覆盖恢复正式数据库。

安装和升级遵循 README。升级前备份并停止服务，保留配置和数据目录，再安装依赖、构建并启动。数据库结构仍为 v47。需要回退时检出兼容版本，在独立目录验证备份后恢复，避免旧代码直接操作不兼容数据。

## 版本定位与边界

`v1.0.0` 为附注标签，指向本次合入 main 的发布提交。使用 `git rev-parse 'v1.0.0^{commit}'` 查看精确冻结提交；不在提交内嵌入自身哈希。推送后核对本地标签、远程标签解引用和远程 main 指向一致。

可信局域网访问者共享部署电脑的数据，v1 没有多用户账号隔离。自动无障碍检查不等同人工 WCAG 认证，数据安全检查不等同完整渗透测试。PDF 内容长度不同仍可能出现较短末页。新供应商深度适配、跨设备同步和新命理模块留给后续版本。
